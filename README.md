The zipped file contains the entire code of the project. the steps to deply the project in our system is by following:
--- git clone the repo [git clone <repo url>]
--- cd <clonned repo> [cd python_based_web_app]
--- tar -xzf secure-electro-store.tar.gz
--- cd secure-electro-store
--- docker compose up -d --build
